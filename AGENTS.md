# Workspace Guidelines

## GitHub Auto-Push Rule
- **Always push to GitHub**: Whenever you make or modify files in this workspace, always stage, commit with a descriptive message, and push to GitHub before finishing:
  ```bash
  git add -A && git commit -m "<descriptive message>" && git push origin main
  ```
